services:
  - type: web
    name: number-classification-api
    env: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/number-classification-api
    envVars:
      - key: PORT
        value: 8080